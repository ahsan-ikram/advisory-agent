[project]
name = "advisory-agent"
version = "0.1.0"
description = "Personal advisory agent powered by LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.20.0",
]
